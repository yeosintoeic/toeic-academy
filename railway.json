{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DATABASE_URL=${DATABASE_URL:-file:/app/dev.db} && (node_modules/.bin/prisma migrate deploy || true) && node_modules/.bin/next start -p ${PORT:-3000}",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
